#include "utils.h"

std::optional<QString> logic::dataParser(const QString &walletFileName)
{
    Q_UNUSED(walletFileName);

    // parseOk ? std::make_optional(std::move(data)) : std::nullopt;

    return QString{};
}
